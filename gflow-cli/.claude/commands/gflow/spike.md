---
description: Gather evidence from the live Flow surface (DOM, network, HAR) before claiming a feature is broken, missing, or impossible.
---

# `/gflow:spike` — measure the blackbox

**Read `skills/spike/SKILL.md` and follow the protocol**, passing `$ARGUMENTS` as the
question to answer.

> Do **not** call `Skill(skill="spike")` — read the file directly.
