---
description: Auto-fix lint and formatting, then report types and tests. Run before every commit.
---

# `/gflow:check` — Quality gates

**Read `skills/check/SKILL.md` and follow the protocol**, passing `$ARGUMENTS` if given.

> Do **not** call `Skill(skill="check")` — read the file directly.
