---
description: Cut a new gflow-cli release — bump version, update CHANGELOG, tag, push, and back-merge.
---

# `/gflow:release` — Cut a new release

**Read `skills/release/SKILL.md` and follow the protocol**, passing `$ARGUMENTS` if given.

> Do **not** call `Skill(skill="release")` — read the file directly.
